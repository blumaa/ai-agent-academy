/node_modules
/coverage
.DS_Store
*.pem
npm-debug.log*
.env*
*.tsbuildinfo

# lesson data (per-project, stays local)
data/

# local claude settings (permissions, MCP config)
.claude/settings.local.json
